# CLAUDE.md

## 提交信息规范

### 格式

```
<前缀>(<范围>): <英文祈使句标题>

- <改动条目>
- <改动条目>

Co-Authored-By: <协作模型> <noreply@anthropic.com>
```

### 标题

- **必须带前缀**，沿用 Conventional Commits：`feat` / `fix` / `refactor` / `perf` / `chore` / `build`
- 可带范围，取模块名或指令名：`fix(rollback): ...`、`feat(tag): ...`
- **标题必须为英文**，冒号后小写开头，用祈使句说明这次改动做了什么
- 单行、不加句号

### 正文

正文用中文，**逐条列点**，每条以 `- ` 开头独占一行，只回答两件事：**改了什么**、**怎么改的**。

- 一条只说一件事，顺带完成的次要改动各占一条
- 不写成整段叙述，也不分多段
- 没有值得列出的内容时，正文可以留空，只保留标题与结尾

以下内容一律不写：

- **修改前的表现**——不出现「原先」「此前」「之前会」这类表述，也不描述旧的实现方式
- **举例**——不放输入输出样例、前后对照、示意代码
- **具体数据**——不写条数、字节数、行号、项目名、用户 ID 等实测数值

### 结尾

保留 `Co-Authored-By` trailer，署名填本次实际使用的模型，不要写死某个版本。
